<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="{{ path('homepage') }}">FakeBNB</a>
  <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
  </button>

  <div class="navbar-collapse collapse" id="navbarColor01" style="">
      <ul class="navbar-nav mr-auto">
          <li class="nav-item">
              <a class="nav-link" href="{{ path('homepage') }}">Home</a>
          </li>
          <li class="nav-item">
              <a class="nav-link" href="{{ path('ads_index') }}">Our ads</a>
          </li>                  
      </ul>
      <ul class="navbar-nav ml-auto">
          {% if app.user %}
            <li class="nav-item dropdown">
                <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="account-dropdown-link">
                    <img src="{{ app.user.avatar }}" alt="{{ app.user.fullName }}'s avatar" class="avatar avatar-mini"> {{ app.user.fullName }}
                </a>

                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="account-dropdown-link">
                    <a href="{{ path('ads_create') }}" class="dropdown-item">Post a new 
                     ad</a>
                    <div class="dropdown-divider"></div>
                    <a href="{{ path('account_index') }}" class="dropdown-item">My account</a>
                    <a href="{{ path('account_profile') }}" class="dropdown-item">Modify account</a>
                    <div class="dropdown-divider"></div>
                    <a href="{{ path('account_logout') }}" class="dropdown-item">Logout</a>
                </div>
            </li>
          {% else %}
            <li class="nav-item">
                <a href="{{ path("account_register") }}" class="nav-link">Register</a>
            </li>
            <li class="nav-item">
                <a href="{{ path("account_login") }}" class="nav-link">Login</a>
            </li>
          {% endif %}
      </ul>
  </div>
</nav>

<!-- Flash messages -->
{% for label, messages in app.flashes %}
  <div class="container">
      <div class="alert alert-{{ label }}">
          {% for message in messages %}
              {{ message | raw }}
          {% endfor %}
      </div>
  </div>
{% endfor %}